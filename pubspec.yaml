name: payeasy

description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.5.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  qr_flutter: ^4.1.0
  intl: ^0.18.1
  razorpay_flutter: ^1.3.6    # ← ADD THIS LINE exactly here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  